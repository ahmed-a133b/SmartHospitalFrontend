# Smart Hospital Voice Assistant Configuration
# Copy this to .env.local and add your Google Cloud TTS API key

# Google Cloud Text-to-Speech API Key
# Get this from Google Cloud Console > APIs & Services > Credentials
# REACT_APP_GOOGLE_TTS_API_KEY=your_api_key_here

# Voice Assistant Default Settings
REACT_APP_TTS_DEFAULT_RATE=1.0
REACT_APP_TTS_DEFAULT_PITCH=1.0
REACT_APP_TTS_DEFAULT_VOICE=en-US-Studio-O
REACT_APP_TTS_ENABLE_BY_DEFAULT=true

# Note: API keys in React apps are visible to users
# For production, consider using a backend proxy for API calls
